RewriteEngine on
# If a directory or a file exists, us it directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# Otherwise forward it to instalador.php
RewriteRule . index.php